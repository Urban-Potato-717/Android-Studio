<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- BookLog는 다크모드에서도 크림 라이트 톤 유지 -->
    <style name="Base.Theme.FinalProject" parent="Theme.Material3.Light.NoActionBar">
        <item name="colorPrimary">@color/brand_brown</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:windowBackground">@color/bg_cream</item>
        <item name="android:statusBarColor">@color/bg_cream</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/card_white</item>
    </style>
</resources>
